[pytest]
pythonpath = .
testpaths = tests
markers =
    smoke: critical happy-path checks
    full: broader end-to-end coverage
    auth: authentication-related tests
    ui: rendering and page-element tests
    chats: chat feature tests
    tools: tool feature tests
    agents: agent feature tests
    token: token-related checks
